# Qwen Code Workbench

Container image that layers `@qwen-code/qwen-code` on top of `ghcr.io/spigell/universal-workbench`.

## Build args

- `UNIVERSAL_WORKBENCH_TAG` (default: `sha-dev`)
- `QWEN_CODE_VERSION` (default: `0.10.6`)
- `SPLITRAIL_VERSION` (default: `3.2.2`)
