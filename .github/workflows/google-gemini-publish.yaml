name: Build and Publish Gemini CLI images
run-name: "${{ github.event.client_payload.message || (github.event_name == 'repository_dispatch' && format('{0}: repository_dispatch by {1}', github.event.action, github.actor)) || format('{0} by {1}', github.event_name, github.actor) }}"

on:
  repository_dispatch:
    types: [universal-workbench-updated]
  push:
    paths:
      - 'google-gemini-docker/**'
      - 'my-gemini-cli-docker/**'
      - '.github/workflows/google-gemini-publish.yaml'
  workflow_dispatch:
    inputs:
      universal_tag:
        description: Universal Workbench tag (optional; falls back to GHCR latest)
        required: false
        type: string

permissions:
  contents: write
  packages: write

jobs:
  resolve-universal-tag:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.final-tag.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/resolve-ghcr-tag
        id: final-tag
        with:
          owner: ${{ github.repository_owner }}
          image: universal-workbench
          payload_tag: ${{ github.event.client_payload.universal_tag }}
          input_tag: ${{ inputs.universal_tag }}
          ghcr_token: ${{ secrets.IMAGES_PUBLISH_TOKEN }}

  build-gemini-workbench:
    needs:
      - resolve-universal-tag
    uses: spigell/my-shared-workflows/.github/workflows/docker-build-release.yaml@main
    secrets:
      gh-token: ${{ secrets.IMAGES_PUBLISH_TOKEN }}
    with:
      image-name: google-gemini-workbench
      context: ./google-gemini-docker
      version: v0.38.1
      build-args: |
        UNIVERSAL_WORKBENCH_TAG=${{ needs.resolve-universal-tag.outputs.tag }}
        GEMINI_CLI_VERSION=0.38.1
      build-contexts: |
        shared=./shared

  build-my-gemini-cli:
    needs:
      - resolve-universal-tag
    uses: spigell/my-shared-workflows/.github/workflows/docker-build-release.yaml@main
    secrets:
      gh-token: ${{ secrets.IMAGES_PUBLISH_TOKEN }}
    with:
      image-name: my-gemini-cli
      context: ./my-gemini-cli-docker
      version: v0.38.1
      build-args: |
        UNIVERSAL_WORKBENCH_TAG=${{ needs.resolve-universal-tag.outputs.tag }}
        GEMINI_CLI_VERSION=0.38.1
        GEMINI_CLI_GIT_URL=https://github.com/spigell/gemini-cli.git
        GEMINI_CLI_GIT_REF=spigell/fix/fallback-family-wrap-v0381-restart
      build-contexts: |
        shared=./shared

  trigger-downstreams:
    needs:
      - build-gemini-workbench
      - build-my-gemini-cli
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch Gemini Workbench Update
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.IMAGES_PUBLISH_TOKEN }}
          event-type: gemini-workbench-updated
          client-payload: '{"gemini_tag":"${{ needs.build-gemini-workbench.outputs.sha-tag }}","my_gemini_cli_tag":"${{ needs.build-my-gemini-cli.outputs.sha-tag }}"}'
