name: Build and Publish GitHub Runner image (Gemini AI Agent)
run-name: "${{ github.event.client_payload.message || format('{0}: repository_dispatch by {1}', github.event.action, github.actor) }}"

on:
  repository_dispatch:
    types: [gemini-workbench-updated]
  push:
    paths:
      - 'github-runner-docker/**'
      - '.github/workflows/github-runner-publish-gemini.yaml'
  workflow_dispatch:
    inputs:
      gemini_tag:
        description: Gemini Workbench tag to consume
        required: false
        type: string
      spigell_gemini_tag:
        description: Spigell Gemini Workbench tag to consume
        required: false
        type: string

permissions:
  contents: write
  packages: write

jobs:
  resolve-base-tags:
    runs-on: ubuntu-latest
    outputs:
      google_tag: ${{ steps.google-tag.outputs.tag }}
      spigell_tag: ${{ steps.spigell-tag.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/resolve-ghcr-tag
        id: google-tag
        with:
          owner: ${{ github.repository_owner }}
          image: google-gemini-workbench
          payload_tag: ${{ github.event.client_payload.gemini_tag }}
          input_tag: ${{ inputs.gemini_tag }}
          ghcr_token: ${{ secrets.IMAGES_PUBLISH_TOKEN }}
      - uses: ./.github/actions/resolve-ghcr-tag
        id: spigell-tag
        with:
          owner: ${{ github.repository_owner }}
          image: spigell-gemini-workbench
          payload_tag: ${{ github.event.client_payload.spigell_gemini_workbench_tag }}
          input_tag: ${{ inputs.spigell_gemini_tag }}
          ghcr_token: ${{ secrets.IMAGES_PUBLISH_TOKEN }}

  build-google-github-runner:
    needs:
      - resolve-base-tags
    uses: spigell/my-shared-workflows/.github/workflows/docker-build-release.yaml@main
    secrets:
      gh-token: ${{ secrets.IMAGES_PUBLISH_TOKEN }}
    with:
      image-name: google-gemini-github-runner
      context: ./github-runner-docker
      version: ${{ needs.resolve-base-tags.outputs.google_tag }}
      build-args: |
        RUNNER_VERSION=2.332.0
        BASE_IMAGE_NAME=google-gemini-workbench
        BASE_IMAGE_TAG=${{ needs.resolve-base-tags.outputs.google_tag }}
      build-contexts: |
        shared=./shared

  build-spigell-github-runner:
    needs:
      - resolve-base-tags
    uses: spigell/my-shared-workflows/.github/workflows/docker-build-release.yaml@main
    secrets:
      gh-token: ${{ secrets.IMAGES_PUBLISH_TOKEN }}
    with:
      image-name: spigell-gemini-github-runner
      context: ./github-runner-docker
      version: ${{ needs.resolve-base-tags.outputs.spigell_tag }}
      build-args: |
        RUNNER_VERSION=2.332.0
        BASE_IMAGE_NAME=spigell-gemini-workbench
        BASE_IMAGE_TAG=${{ needs.resolve-base-tags.outputs.spigell_tag }}
      build-contexts: |
        shared=./shared
